# leetcode75
leetcode 75 problems and solution using c#

Note: Make sure you configure your git `user.name` and `user.email` before committing. For example:

```
git config --global user.name "Your Name"
git config --global user.email "you@example.com"
```

This ensures commits are attributed correctly.
